# Pass the Ticket (PtT) from Linux

Although not common, Linux computers can connect to Active Directory to provide centralized identity management and integrate with the organization's systems, giving users the ability to have a single identity to authenticate on Linux and Windows computers.

A Linux computer connected to Active Directory commonly uses Kerberos as authentication. Suppose this is the case, and we manage to compromise a Linux machine connected to Active Directory. In that case, we could try to find Kerberos tickets to impersonate other users and gain more access to the network.  A Linux system can be configured in various ways to store Kerberos tickets. We'll discuss a few different storage options in this section.

***Note:** A Linux machine not connected to Active Directory could use Kerberos tickets in scripts or to authenticate to the network. It is not a requirement to be joined to the domain to use Kerberos tickets from a Linux machine.*

## Kerberos on Linux

Windows and Linux use the same process to request a Ticket Granting Ticket (TGT) and Service Ticket (TGS). However, how they store the ticket information may vary depending on the Linux distribution and implementation.  In most cases, Linux machines store Kerberos tickets as ccache files in the /tmp directory. By default, the location of the Kerberos ticket is stored in the environment variable KRB5CCNAME. This variable can identify if Kerberos tickets are being used or if the default location for storing Kerberos tickets is changed. These ccache files are protected by reading and write permissions, but a user with elevated privileges or root privileges could easily gain access to these tickets.  Another everyday use of Kerberos in Linux is with keytab files. A keytab is a file containing pairs of Kerberos principals and encrypted keys (which are derived from the Kerberos password). You can use a keytab file to authenticate to various remote systems using Kerberos without entering a password. However, when you change your password, you must recreate all your keytab files.  Keytab files commonly allow scripts to authenticate automatically using Kerberos without requiring human interaction or access to a password stored in a plain text file. For example, a script can use a keytab file to access files stored in the Windows share folder.

If none of the above solutions worked, let's try debugging further:  1. Check Clipboard Contents It’s possible that the clipboard is storing the text without markdown formatting. To check:  Paste the copied text into a plain text editor (like nano, vim, gedit, or Notepad). See if the headings are formatted as # Heading (Markdown) or just plain text. If the headings are missing #, then the issue is with how the website copies content.  2. Try Copying from Different Sources Copy from a different website and paste it into Obsidian. Copy from Obsidian itself and paste it back to check if the issue is specific to external sources. 3. Test with Obsidian's "Paste Special" (If Available) Try using Ctrl + Shift + V (Linux/Windows) or Cmd + Shift + V (Mac) to paste without formatting. 4. Force Markdown Pasting If Obsidian is treating everything as plain text, manually convert it:  Paste content into a markdown editor like Dillinger.io and check if it retains headings. If it does, copy from there and paste into Obsidian. If not, then the website isn't copying proper markdown. 5. Check for an Obsidian Update or Bug Go to Settings > About and check if an update is available. If this started happening after an update, it could be a bug—check the Obsidian forum or Discord. 6. Use a Clipboard Manager Since you’re on Linux, try installing copyq:  sh Copy Edit sudo apt install copyq copyq Copy the content, then open copyq to see how it’s being stored. If it’s losing Markdown formatting, the issue is with how the website copies data. 7. Workaround: Convert HTML to Markdown If the copied content is HTML, you can convert it to Markdown before pasting:  sh Copy Edit pandoc -f html -t markdown -o output.md input.html Save copied text as input.html and run the command above. Open output.md in Obsidian. If none of this works, let me know:  The website you’re copying from. If this happens for all sites or just one. If it happens only in Obsidian or other Markdown editors too. That way, we can narrow down the root cause! 🚀

6. Use a Clipboard Manager Since you’re on Linux, try installing copyq:  sh Copy Edit sudo apt install copyq copyq Copy the content, then open copyq to see how it’s being stored. If it’s losing Markdown formatting, the issue is with how the website copies data. 7. Workaround: Convert HTML to Markdown If the copied content is HTML, you can convert it to Markdown before pasting:  sh Copy Edit pandoc -f html -t markdown -o output.md input.html Save copied text as input.html and run the command above. Open output.md in Obsidian.
6. Use a Clipboard Manager Since you’re on Linux, try installing copyq:  sh Copy Edit sudo apt install copyq copyq Copy the content, then open copyq to see how it’s being stored. If it’s losing Markdown formatting, the issue is with how the website copies data. 7. Workaround: Convert HTML to Markdown If the copied content is HTML, you can convert it to Markdown before pasting:  sh Copy Edit pandoc -f html -t markdown -o output.md input.html Save copied text as input.html and run the command above. Open output.md in Obsidian.

6. Use a Clipboard Manager Since you’re on Linux, try installing copyq:  sh Copy Edit sudo apt install copyq copyq Copy the content, then open copyq to see how it’s being stored. If it’s losing Markdown formatting, the issue is with how the website copies data. 7. Workaround: Convert HTML to Markdown If the copied content is HTML, you can convert it to Markdown before pasting:  sh Copy Edit pandoc -f html -t markdown -o output.md input.html Save copied text as input.html and run the command above. Open output.md in Obsidian.
The output of the command indicates that the machine is configured as a Kerberos member. It also gives us information about the domain name (inlanefreight.htb) and which users and groups are permitted to log in, which in this case are the users David and Julio and the group Linux Admins.  In case realm is not available, we can also look for other tools used to integrate Linux with Active Directory such as sssd or winbind. Looking for those services running in the machine is another way to identify if it is domain joined. We can read this blog post for more details. Let's search for those services to confirm if the machine is domain joined.  PS - Check if Linux Machine is Domain Joined   Pass the Ticket (PtT) from Linux david@inlanefreight.htb@linux01:~$ ps -ef | grep -i "winbind\|sssd"  root        2140       1  0 Sep29 ?        00:00:01 /usr/sbin/sssd -i --logger=files root        2141    2140  0 Sep29 ?        00:00:08 /usr/libexec/sssd/sssd_be --domain inlanefreight.htb --uid 0 --gid 0 --logger=files root        2142    2140  0 Sep29 ?        00:00:03 /usr/libexec/sssd/sssd_nss --uid 0 --gid 0 --logger=files root        2143    2140  0 Sep29 ?        00:00:03 /usr/libexec/sssd/sssd_pam --uid 0 --gid 0 --logger=files


The output of the command indicates that the machine is configured as a Kerberos member. It also gives us information about the domain name (inlanefreight.htb) and which users and groups are permitted to log in, which in this case are the users David and Julio and the group Linux Admins.  In case realm is not available, we can also look for other tools used to integrate Linux with Active Directory such as sssd or winbind. Looking for those services running in the machine is another way to identify if it is domain joined. We can read this blog post for more details. Let's search for those services to confirm if the machine is domain joined.  PS - Check if Linux Machine is Domain Joined   Pass the Ticket (PtT) from Linux david@inlanefreight.htb@linux01:~$ ps -ef | grep -i "winbind\|sssd"  root        2140       1  0 Sep29 ?        00:00:01 /usr/sbin/sssd -i --logger=files root        2141    2140  0 Sep29 ?        00:00:08 /usr/libexec/sssd/sssd_be --domain inlanefreight.htb --uid 0 --gid 0 --logger=files root        2142    2140  0 Sep29 ?        00:00:03 /usr/libexec/sssd/sssd_nss --uid 0 --gid 0 --logger=files root        2143    2140  0 Sep29 ?        00:00:03 /usr/libexec/sssd/sssd_pam --uid 0 --gid 0 --logger=files

Share   You said: when i am copying the content from a website i and pasting it in obsidian it is not converting headingi into heading like it used to do earlier ChatGPT said: It sounds like Obsidian isn't recognizing the formatting of headings when pasting content. A few things might be causing this:  Possible Causes & Fixes Paste as Plain Text  Try Ctrl + Shift + V (or Cmd + Shift + V on macOS) to paste as plain text and manually format it. Check "Smart Pasting" in Obsidian  Go to Settings > Editor and check if there’s an option for smart pasting or auto-formatting. Website Formatting Changed  Some websites use CSS to style headings instead of actual <h1>–<h6> tags, which might not copy correctly. Try copying from a different website to see if the issue persists. Use Markdown Conversion Tools  If Obsidian isn't detecting the headings, paste the content into a markdown converter (like dillinger.io or markdowntohtml.com) and then copy the markdown output to Obsidian. Check if Plugins or Settings Changed  If you recently updated Obsidian or installed/uninstalled plugins, try disabling all community plugins and restarting Obsidian. Test in a Different Note  Create a new note and paste the content to see if it's note-specific. Let me know what happens when you try these! 🚀        You said: nothing is working ChatGPT said: If none of the above solutions worked, let's try debugging further:  1. Check Clipboard Contents It’s possible that the clipboard is storing the text without markdown formatting. To check:  Paste the copied text into a plain text editor (like nano, vim, gedit, or Notepad). See if the headings are formatted as # Heading (Markdown) or just plain text. If the headings are missing #, then the issue is with how the website copies content.  2. Try Copying from Different Sources Copy from a different website and paste it into Obsidian. Copy from Obsidian itself and paste it back to check if the issue is specific to external sources. 3. Test with Obsidian's "Paste Special" (If Available) Try using Ctrl + Shift + V (Linux/Windows) or Cmd + Shift + V (Mac) to paste without formatting. 4. Force Markdown Pasting If Obsidian is treating everything as plain text, manually convert it:  Paste content into a markdown editor like Dillinger.io and check if it retains headings. If it does, copy from there and paste into Obsidian. If not, then the website isn't copying proper markdown. 5. Check for an Obsidian Update or Bug Go to Settings > About and check if an update is available. If this started happening after an update, it could be a bug—check the Obsidian forum or Discord. 6. Use a Clipboard Manager Since you’re on Linux, try installing copyq:  sh Copy Edit sudo apt install copyq copyq Copy the content, then open copyq to see how it’s being stored. If it’s losing Markdown formatting, the issue is with how the website copies data. 7. Workaround: Convert HTML to Markdown If the copied content is HTML, you can convert it to Markdown before pasting:  sh Copy Edit pandoc -f html -t markdown -o output.md input.html Save copied text as input.html and run the command above. Open output.md in Obsidian. If none of this works, let me know:  The website you’re copying from. If this happens for all sites or just one. If it happens only in Obsidian or other Markdown editors too. That way, we can narrow down the root cause! 🚀        You said: even chatgpt text can be interpretted by obsidian but it is not doing it now ChatGPT said: Since even ChatGPT's formatted text is no longer being interpreted correctly in Obsidian, the issue is likely with Obsidian's settings or a clipboard-related problem. Let's go step by step to troubleshoot this:  1. Restart Obsidian & Your System Close and restart Obsidian. If that doesn’t work, restart your system. 2. Test in a New Vault Create a new Obsidian vault (File > New Vault) and paste into a new note. If it works there, the issue might be related to a setting or plugin in your main vault. 3. Disable All Plugins (Temporarily) Go to Settings > Community Plugins Disable all plugins Restart Obsidian and test again. If this fixes the issue, one of your plugins is interfering—enable them one by one to find the culprit. 4. Check the "Editor" Settings Go to Settings > Editor Look for options related to pasting and formatting. Try toggling "Strict line breaks" or other formatting options. 5. Check if the Clipboard is Stripping Formatting Run the following command to see how the clipboard stores the copied content:  sh Copy Edit xclip -selection clipboard -o or  sh Copy Edit xsel --clipboard --output If it shows plain text instead of Markdown-formatted text (# Heading), the issue is with the clipboard. 6. Try Copying & Pasting in a Different Markdown Editor Open a different Markdown editor (like Typora, MarkText, or VS Code). Paste the text there. If the issue persists, your clipboard may be stripping Markdown formatting. 7. Use an Alternative Copying Method Instead of Ctrl + C, try right-click > Copy and then paste into Obsidian. Try using a clipboard manager (like CopyQ) to see if formatting is preserved. 8. Reinstall Obsidian If nothing else works, backup your vault, uninstall Obsidian, and reinstall it. If the problem persists, let me know:  If this happens in other Markdown editors. If copying from different sources (ChatGPT, websites, etc.) behaves differently. If you see any changes in xclip output. That will help pinpoint the issue! 🚀

Share   You said: when i am copying the content from a website i and pasting it in obsidian it is not converting headingi into heading like it used to do earlier ChatGPT said: It sounds like Obsidian isn't recognizing the formatting of headings when pasting content. A few things might be causing this:  Possible Causes & Fixes Paste as Plain Text  Try Ctrl + Shift + V (or Cmd + Shift + V on macOS) to paste as plain text and manually format it. Check "Smart Pasting" in Obsidian  Go to Settings > Editor and check if there’s an option for smart pasting or auto-formatting. Website Formatting Changed  Some websites use CSS to style headings instead of actual <h1>–<h6> tags, which might not copy correctly. Try copying from a different website to see if the issue persists. Use Markdown Conversion Tools  If Obsidian isn't detecting the headings, paste the content into a markdown converter (like dillinger.io or markdowntohtml.com) and then copy the markdown output to Obsidian. Check if Plugins or Settings Changed  If you recently updated Obsidian or installed/uninstalled plugins, try disabling all community plugins and restarting Obsidian. Test in a Different Note  Create a new note and paste the content to see if it's note-specific. Let me know what happens when you try these! 🚀        You said: nothing is working ChatGPT said: If none of the above solutions worked, let's try debugging further:  1. Check Clipboard Contents It’s possible that the clipboard is storing the text without markdown formatting. To check:  Paste the copied text into a plain text editor (like nano, vim, gedit, or Notepad). See if the headings are formatted as # Heading (Markdown) or just plain text. If the headings are missing #, then the issue is with how the website copies content.  2. Try Copying from Different Sources Copy from a different website and paste it into Obsidian. Copy from Obsidian itself and paste it back to check if the issue is specific to external sources. 3. Test with Obsidian's "Paste Special" (If Available) Try using Ctrl + Shift + V (Linux/Windows) or Cmd + Shift + V (Mac) to paste without formatting. 4. Force Markdown Pasting If Obsidian is treating everything as plain text, manually convert it:  Paste content into a markdown editor like Dillinger.io and check if it retains headings. If it does, copy from there and paste into Obsidian. If not, then the website isn't copying proper markdown. 5. Check for an Obsidian Update or Bug Go to Settings > About and check if an update is available. If this started happening after an update, it could be a bug—check the Obsidian forum or Discord. 6. Use a Clipboard Manager Since you’re on Linux, try installing copyq:  sh Copy Edit sudo apt install copyq copyq Copy the content, then open copyq to see how it’s being stored. If it’s losing Markdown formatting, the issue is with how the website copies data. 7. Workaround: Convert HTML to Markdown If the copied content is HTML, you can convert it to Markdown before pasting:  sh Copy Edit pandoc -f html -t markdown -o output.md input.html Save copied text as input.html and run the command above. Open output.md in Obsidian. If none of this works, let me know:  The website you’re copying from. If this happens for all sites or just one. If it happens only in Obsidian or other Markdown editors too. That way, we can narrow down the root cause! 🚀        You said: even chatgpt text can be interpretted by obsidian but it is not doing it now ChatGPT said: Since even ChatGPT's formatted text is no longer being interpreted correctly in Obsidian, the issue is likely with Obsidian's settings or a clipboard-related problem. Let's go step by step to troubleshoot this:  1. Restart Obsidian & Your System Close and restart Obsidian. If that doesn’t work, restart your system. 2. Test in a New Vault Create a new Obsidian vault (File > New Vault) and paste into a new note. If it works there, the issue might be related to a setting or plugin in your main vault. 3. Disable All Plugins (Temporarily) Go to Settings > Community Plugins Disable all plugins Restart Obsidian and test again. If this fixes the issue, one of your plugins is interfering—enable them one by one to find the culprit. 4. Check the "Editor" Settings Go to Settings > Editor Look for options related to pasting and formatting. Try toggling "Strict line breaks" or other formatting options. 5. Check if the Clipboard is Stripping Formatting Run the following command to see how the clipboard stores the copied content:  sh Copy Edit xclip -selection clipboard -o or  sh Copy Edit xsel --clipboard --output If it shows plain text instead of Markdown-formatted text (# Heading), the issue is with the clipboard. 6. Try Copying & Pasting in a Different Markdown Editor Open a different Markdown editor (like Typora, MarkText, or VS Code). Paste the text there. If the issue persists, your clipboard may be stripping Markdown formatting. 7. Use an Alternative Copying Method Instead of Ctrl + C, try right-click > Copy and then paste into Obsidian. Try using a clipboard manager (like CopyQ) to see if formatting is preserved. 8. Reinstall Obsidian If nothing else works, backup your vault, uninstall Obsidian, and reinstall it. If the problem persists, let me know:  If this happens in other Markdown editors. If copying from different sources (ChatGPT, websites, etc.) behaves differently. If you see any changes in xclip output. That will help pinpoint the issue! 🚀



Share   You said: when i am copying the content from a website i and pasting it in obsidian it is not converting headingi into heading like it used to do earlier ChatGPT said: It sounds like Obsidian isn't recognizing the formatting of headings when pasting content. A few things might be causing this:  Possible Causes & Fixes Paste as Plain Text  Try Ctrl + Shift + V (or Cmd + Shift + V on macOS) to paste as plain text and manually format it. Check "Smart Pasting" in Obsidian  Go to Settings > Editor and check if there’s an option for smart pasting or auto-formatting. Website Formatting Changed  Some websites use CSS to style headings instead of actual <h1>–<h6> tags, which might not copy correctly. Try copying from a different website to see if the issue persists. Use Markdown Conversion Tools  If Obsidian isn't detecting the headings, paste the content into a markdown converter (like dillinger.io or markdowntohtml.com) and then copy the markdown output to Obsidian. Check if Plugins or Settings Changed  If you recently updated Obsidian or installed/uninstalled plugins, try disabling all community plugins and restarting Obsidian. Test in a Different Note  Create a new note and paste the content to see if it's note-specific. Let me know what happens when you try these! 🚀        You said: nothing is working ChatGPT said: If none of the above solutions worked, let's try debugging further:  1. Check Clipboard Contents It’s possible that the clipboard is storing the text without markdown formatting. To check:  Paste the copied text into a plain text editor (like nano, vim, gedit, or Notepad). See if the headings are formatted as # Heading (Markdown) or just plain text. If the headings are missing #, then the issue is with how the website copies content.  2. Try Copying from Different Sources Copy from a different website and paste it into Obsidian. Copy from Obsidian itself and paste it back to check if the issue is specific to external sources. 3. Test with Obsidian's "Paste Special" (If Available) Try using Ctrl + Shift + V (Linux/Windows) or Cmd + Shift + V (Mac) to paste without formatting. 4. Force Markdown Pasting If Obsidian is treating everything as plain text, manually convert it:  Paste content into a markdown editor like Dillinger.io and check if it retains headings. If it does, copy from there and paste into Obsidian. If not, then the website isn't copying proper markdown. 5. Check for an Obsidian Update or Bug Go to Settings > About and check if an update is available. If this started happening after an update, it could be a bug—check the Obsidian forum or Discord. 6. Use a Clipboard Manager Since you’re on Linux, try installing copyq:  sh Copy Edit sudo apt install copyq copyq Copy the content, then open copyq to see how it’s being stored. If it’s losing Markdown formatting, the issue is with how the website copies data. 7. Workaround: Convert HTML to Markdown If the copied content is HTML, you can convert it to Markdown before pasting:  sh Copy Edit pandoc -f html -t markdown -o output.md input.html Save copied text as input.html and run the command above. Open output.md in Obsidian. If none of this works, let me know:  The website you’re copying from. If this happens for all sites or just one. If it happens only in Obsidian or other Markdown editors too. That way, we can narrow down the root cause! 🚀        You said: even chatgpt text can be interpretted by obsidian but it is not doing it now ChatGPT said: Since even ChatGPT's formatted text is no longer being interpreted correctly in Obsidian, the issue is likely with Obsidian's settings or a clipboard-related problem. Let's go step by step to troubleshoot this:  1. Restart Obsidian & Your System Close and restart Obsidian. If that doesn’t work, restart your system. 2. Test in a New Vault Create a new Obsidian vault (File > New Vault) and paste into a new note. If it works there, the issue might be related to a setting or plugin in your main vault. 3. Disable All Plugins (Temporarily) Go to Settings > Community Plugins Disable all plugins Restart Obsidian and test again. If this fixes the issue, one of your plugins is interfering—enable them one by one to find the culprit. 4. Check the "Editor" Settings Go to Settings > Editor Look for options related to pasting and formatting. Try toggling "Strict line breaks" or other formatting options. 5. Check if the Clipboard is Stripping Formatting Run the following command to see how the clipboard stores the copied content:  sh Copy Edit xclip -selection clipboard -o or  sh Copy Edit xsel --clipboard --output If it shows plain text instead of Markdown-formatted text (# Heading), the issue is with the clipboard. 6. Try Copying & Pasting in a Different Markdown Editor Open a different Markdown editor (like Typora, MarkText, or VS Code). Paste the text there. If the issue persists, your clipboard may be stripping Markdown formatting. 7. Use an Alternative Copying Method Instead of Ctrl + C, try right-click > Copy and then paste into Obsidian. Try using a clipboard manager (like CopyQ) to see if formatting is preserved. 8. Reinstall Obsidian If nothing else works, backup your vault, uninstall Obsidian, and reinstall it. If the problem persists, let me know:  If this happens in other Markdown editors. If copying from different sources (ChatGPT, websites, etc.) behaves differently. If you see any changes in xclip output. That will help pinpoint the issue! 🚀




Share   You said: when i am copying the content from a website i and pasting it in obsidian it is not converting headingi into heading like it used to do earlier ChatGPT said: It sounds like Obsidian isn't recognizing the formatting of headings when pasting content. A few things might be causing this:  Possible Causes & Fixes Paste as Plain Text  Try Ctrl + Shift + V (or Cmd + Shift + V on macOS) to paste as plain text and manually format it. Check "Smart Pasting" in Obsidian  Go to Settings > Editor and check if there’s an option for smart pasting or auto-formatting. Website Formatting Changed  Some websites use CSS to style headings instead of actual <h1>–<h6> tags, which might not copy correctly. Try copying from a different website to see if the issue persists. Use Markdown Conversion Tools  If Obsidian isn't detecting the headings, paste the content into a markdown converter (like dillinger.io or markdowntohtml.com) and then copy the markdown output to Obsidian. Check if Plugins or Settings Changed  If you recently updated Obsidian or installed/uninstalled plugins, try disabling all community plugins and restarting Obsidian. Test in a Different Note  Create a new note and paste the content to see if it's note-specific. Let me know what happens when you try these! 🚀        You said: nothing is working ChatGPT said: If none of the above solutions worked, let's try debugging further:  1. Check Clipboard Contents It’s possible that the clipboard is storing the text without markdown formatting. To check:  Paste the copied text into a plain text editor (like nano, vim, gedit, or Notepad). See if the headings are formatted as # Heading (Markdown) or just plain text. If the headings are missing #, then the issue is with how the website copies content.  2. Try Copying from Different Sources Copy from a different website and paste it into Obsidian. Copy from Obsidian itself and paste it back to check if the issue is specific to external sources. 3. Test with Obsidian's "Paste Special" (If Available) Try using Ctrl + Shift + V (Linux/Windows) or Cmd + Shift + V (Mac) to paste without formatting. 4. Force Markdown Pasting If Obsidian is treating everything as plain text, manually convert it:  Paste content into a markdown editor like Dillinger.io and check if it retains headings. If it does, copy from there and paste into Obsidian. If not, then the website isn't copying proper markdown. 5. Check for an Obsidian Update or Bug Go to Settings > About and check if an update is available. If this started happening after an update, it could be a bug—check the Obsidian forum or Discord. 6. Use a Clipboard Manager Since you’re on Linux, try installing copyq:  sh Copy Edit sudo apt install copyq copyq Copy the content, then open copyq to see how it’s being stored. If it’s losing Markdown formatting, the issue is with how the website copies data. 7. Workaround: Convert HTML to Markdown If the copied content is HTML, you can convert it to Markdown before pasting:  sh Copy Edit pandoc -f html -t markdown -o output.md input.html Save copied text as input.html and run the command above. Open output.md in Obsidian. If none of this works, let me know:  The website you’re copying from. If this happens for all sites or just one. If it happens only in Obsidian or other Markdown editors too. That way, we can narrow down the root cause! 🚀        You said: even chatgpt text can be interpretted by obsidian but it is not doing it now ChatGPT said: Since even ChatGPT's formatted text is no longer being interpreted correctly in Obsidian, the issue is likely with Obsidian's settings or a clipboard-related problem. Let's go step by step to troubleshoot this:  1. Restart Obsidian & Your System Close and restart Obsidian. If that doesn’t work, restart your system. 2. Test in a New Vault Create a new Obsidian vault (File > New Vault) and paste into a new note. If it works there, the issue might be related to a setting or plugin in your main vault. 3. Disable All Plugins (Temporarily) Go to Settings > Community Plugins Disable all plugins Restart Obsidian and test again. If this fixes the issue, one of your plugins is interfering—enable them one by one to find the culprit. 4. Check the "Editor" Settings Go to Settings > Editor Look for options related to pasting and formatting. Try toggling "Strict line breaks" or other formatting options. 5. Check if the Clipboard is Stripping Formatting Run the following command to see how the clipboard stores the copied content:  sh Copy Edit xclip -selection clipboard -o or  sh Copy Edit xsel --clipboard --output If it shows plain text instead of Markdown-formatted text (# Heading), the issue is with the clipboard. 6. Try Copying & Pasting in a Different Markdown Editor Open a different Markdown editor (like Typora, MarkText, or VS Code). Paste the text there. If the issue persists, your clipboard may be stripping Markdown formatting. 7. Use an Alternative Copying Method Instead of Ctrl + C, try right-click > Copy and then paste into Obsidian. Try using a clipboard manager (like CopyQ) to see if formatting is preserved. 8. Reinstall Obsidian If nothing else works, backup your vault, uninstall Obsidian, and reinstall it. If the problem persists, let me know:  If this happens in other Markdown editors. If copying from different sources (ChatGPT, websites, etc.) behaves differently. If you see any changes in xclip output. That will help pinpoint the issue! 🚀